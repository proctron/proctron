{
  "name": "proctron",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "dependencies": {
    "turbo": "^1.6.3"
  },
  "workspaces": [
    "packages/*",
    "shared/*",
    "utils/*"
  ],
  "private": true
}
